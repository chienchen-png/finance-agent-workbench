#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
文档鉴别材料 —— 软件说明书内容
================================
整合：用户使用说明书 + 立项汇报方案 + 项目架构文档 + 操作说明书
目标：60 页完整软件说明书。内容为项目真实情况，已尽量避免编造。

本文件只提供 markdown 内容字符串，由主脚本调用 _md_to_docx.md_to_docx() 转换。
"""

DOC_MD = r"""# 基于自然语言操控的财务标准化作业执行系统 软件说明书

**版本：V1.0.0**

| 项目 | 说明 |
|------|------|
| 软件名称 | 基于自然语言操控的财务标准化作业执行系统 |
| 软件简称 | 财务标准化作业系统 |
| 版本号 | V1.0.0 |
| 开发语言 | Python 3.14 + JavaScript / TypeScript |
| 运行环境 | Windows 10 / 11（64 位），Chrome / Edge ≥ 115 |
| 软件形态 | 浏览器 Web 应用（内网离线部署） |

---

## 第一章 软件概述

### 1.1 软件定位

**基于自然语言操控的财务标准化作业执行系统**（以下简称"本系统"）是面向财务人员的一款**智能财务工作台**。它把财务日常繁杂的 Excel 数据核对、分析、报表等手工工作，改造为 **"文件上传 → 自然语言指令 → 系统自动执行 → 报告输出"** 的标准化、可审计工作流。

本系统不是通用聊天机器人，而是**"Excel 数据核对 + 受控写回"的垂直自动化平台**，并演进为**「对话 + 集成应用」双形态**：

- **对话工作台（项目空间）**：承接长尾、自由、临时性任务。用户用自然语言下达指令，AI 智能体读取 Excel、导入数据平面、分析、修改，确认后写回原文件。
- **集成应用**：把高频、固定的业务流程固化为向导式应用，业务人员按步骤操作即可产出专业结果，无需建模/编程知识。

### 1.2 开发背景

公司财务部门日常面临大量 Excel 数据核对、分析、报表工作。以销售回款提成核对为例，每季度需将季度分表与年度汇总逐条比对，核对维度涵盖合同编号、销售人员、客户单位及金额等要素。当前工作流程存在以下突出痛点：

- **数据分散核对困难**：季度分表与汇总表分属不同文件，字段命名不统一，核对低效易错。
- **人工重复标准不一**：每季度重复相同流程，不同人员标准不一，记录格式不统一。
- **错误回溯困难**：修改后难以追溯原始值、操作人、时间，回滚复杂。
- **分析门槛高**：从数据到归因建议依赖专业建模能力，业务人员无法自助。
- **报告生成耗时**：核对/分析后手工撰写报告，格式内容难一致。

### 1.3 建设目标

本系统旨在构建一个财务智能工作台平台，具体目标包括：

- 实现 Excel 自动导入、结构化存储与字段识别（含表头智能定位）。
- 建立多层匹配逻辑，自动完成核对与五级差异分级。
- 在临时库隔离 + 字段级修改日志机制下执行数据修正，只改勾选单元格。
- 按统一模板自动生成核对报告 / 建模分析报告（可导出 Word，含图表、三线表排版）。
- 将高频流程固化为集成应用，业务人员无需建模/脚本知识即可产出专业报告。
- 建立完整操作审计日志，实现责任可追溯。

### 1.4 业务价值

| 价值维度 | 具体表现 |
|---------|---------|
| 效率提升 | 将原需数小时的手工逐条核对/分析压缩至分钟级自动完成 |
| 质量保障 | 标准化匹配逻辑 + 五级差异分级，消除人为疏漏，规则一致执行 |
| 风险控制 | 临时库隔离防污染主库、字段级修改日志全链路留痕、写回仅改勾选单元格 |
| 降低门槛 | 业务人员无需建模/编程知识，一句话即可产出建模分析与报告 |
| 成本节约 | 减少重复性人力投入；本地确定性计算大幅降低 Token 消耗 |
| 可复用性 | 支持多年度、多项目、多应用独立运行，一次部署长期使用 |

### 1.5 适用范围

本系统定位为财务部门内部智能工作台，核心是"AI 辅助决策、本地确定计算、全程可审计"。系统不替代财务人员的专业判断，而是将规则明确、重复性高的核对与分析自动化，财务人员仅在关键节点做决策。适用于：

- 销售回款提成的季度核对与年度汇总。
- 其他"明细表 vs 汇总表"模式的财务数据核对。
- 财务数据建模、趋势分析与报告生成等周期性工作。

### 1.6 软件整体工作流程

本系统的使用遵循"文件上传 → 自然语言指令 → 系统自动执行 → 报告输出"的闭环，其整体数据流如下：

| 阶段 | 参与者 | 动作 | 结果 |
|------|--------|------|------|
| 数据准备 | 用户 | 上传 Excel、确认表头锚点 | 数据物化为内部数据库表 |
| 目标描述 | 用户 | 用一句话描述核对/分析目标 | 生成任务意图 |
| 模型匹配 | AI + 本地 | 智能体理解意图，匹配工具/模型 | 确定执行方案 |
| 确定性执行 | 本地引擎 | 调用 Python 算法完成核对/建模/图表 | 出结果与五级差异分级 |
| 人工决策 | 用户 | 对待审计项 / 差异做判断 | 确定写回范围 |
| 写回留痕 | 本地引擎 | 只改勾选单元格，备份原文件 | Excel 更新 + 修改日志 |
| 报告生成 | AI + 本地 | 按模板撰写报告 | 可导出 Word/PDF |

这一流程确保每个环节都有数据支撑、可追溯，且把"用户决策"和"机器计算"边界清晰分离——机器负责确定性、重复性的计算，用户负责需要专业判断的业务决策。

### 1.7 软件功能总览

本系统的功能可归为四大类：

| 功能类别 | 说明 | 承接形态 |
|---------|------|---------|
| 项目空间 | 文件管理 + 对话式操作 | 对话工作台 |
| 数据核对 | 多表金额核对、差异分级、审计闭环 | 集成应用一 |
| 财务建模 | 多文件合并、模型库建模、图表趋势 | 集成应用二 |
| 报告产出 | 核对报告 / 分析报告导出 Word | 两个应用皆有 |

**核心能力清单**（用自然语言即可触发）：
- 项目文件 / Excel 分析
- 两表差异核对
- 图表生成（趋势 / 对比 / 占比）
- 财务建模（32 个模型）
- 数据写回（受控、可审计）
- 报告导出（Word / PDF / HTML）

### 1.8 设计原则

本系统的设计遵循以下原则：

| 原则 | 解释 |
|------|------|
| 确定性优先 | 凡能用算法确定的，不用 AI 猜测；AI 只做语义理解、归因、建议。 |
| 可审计性 | 每一步操作留痕（字段级日志、运行记录、报告来源），责任可回溯。 |
| 安全隔离 | 数据在临时库快照内处理，写回前备份，只改勾选单元格。 |
| 复用优先 | 尽量复用主工作台的基础设施（存储、文件、前端组件），降低维护成本。 |
| 零配置部署 | 内置环境、服务自动选端口、浏览器登录即用，无需安装依赖。 |
| 可插拔扩展 | 新增业务规则以 Skill / 模型 / 图表配置方式扩展，不改核心代码。 |

---

## 第二章 软件架构

### 2.1 全局架构

本系统采用分层架构，自上而下分为六层：交互层、AI 模型层、应用层、工具层、存储层、知识/配置层。

| 层 | 核心职责 | 关键约束 |
|----|---------|---------|
| 交互层 | 浏览器界面（工作台总览 / AI 配置 / 项目空间 / 集成应用一 / 集成应用二） | 图形化，无需命令行 |
| AI 模型层 | 意图理解、引导、归因、报告 | 仅处理文本，不执行确定性计算 |
| 应用层 | 统一引擎承载两种形态（对话 + 集成应用） | 复用主工作台基础设施 |
| 工具层 | 确定性计算（秒级、0 Token） | 纯算法，不消耗 Token |
| 存储层 | 数据平面 + 临时库快照 + 变量索引 | 数据按 project_id 隔离 |
| 知识/配置层 | Skill / 模型目录 / 报告模板 / 字段映射 | 声明式，可插拔 |

### 2.2 智能体框架

智能体是本系统的"大脑"，其框架设计如下：

- **三层渐进式加载**：L0（agent.md 常驻）/ L1（SKILL.md 按需）/ L2（资源用时读），对齐 VS Code Copilot 三层模型，省算力。
- **本地确定性计算 + AI 辅助**：核对/分级/建模/图表优先调用本地 Python 脚本；AI 只做引导/归因/报告。
- **工具调用能力**：30 类工具，灵活可扩展。
- **沙箱 + 多工作模式**：文件工具限定项目工作目录；人工审批 / 全自动双模式。
- **全链路可审计**：字段级修改日志、会话计时、本地操作日志、活动 run 注册表。

### 2.3 Skill 体系

Skill 是承载财务方法论的"智能模块"，设计如下：

- **声明式、可插拔**：每次业务方法论浓缩为一个 Skill 及其资源文件，不改核心代码。
- **每个集成应用适配专属 Skill**：应用一装 data-reconcile skill（数据核对专用工具）、应用二装 financial-modeling skill（财务建模专用工具），收敛 AI 职责边界。
- **领域知识丰富**：32 财务模型 A-G 分类 + 24 图表模板 L1/L2/L3 档位。

### 2.4 隐藏项目组件（核心架构）

**这是本系统的架构精髓。** 每个 AI 集成应用本质上都是"隐藏项目组件"——它不是独立的应用框架，而是一套前端页面架设在"一个隐藏项目"之上，复用主工作台的项目/存储/文件/前端能力。

一个集成应用 = 三件套：

| 组成 | 说明 | 示例 |
|------|------|------|
| 隐藏项目 | projects 表一条 `__app_*` 前缀记录，前端显式复用 | `__app_recon__` / `__app_finmod__` |
| 绑定项目目录 | 隐藏项目的 work_dir，专属产物目录 | data/app-workspaces/recon / finmod |
| 绑定项目数据库 | 统一 data/app.db 按 project_id 隔离的数据平面 | 同一 app.db，不同 project_id |

**为什么巧妙**：

- **修 bug = 修前端**——应用本质是前端页面 + 后端几个 API 端点，架在"隐藏项目"这个容器上。
- **完全复用主工作台**：隐藏项目复用了 storage/*、routes/files.py、routes/project_data.py、AppDataPanel、FileEditor 等全部既有基础设施。
- **隔离与一致性并存**：各应用数据按 project_id 隔离，但都沉淀在同一 app.db（统一迁移、统一备份、统一日志）。

### 2.5 技术选型

| 技术 | 用途 | 说明 |
|------|------|------|
| Python 3.14 + Flask | 后端运行环境 | 开源 |
| SQLite | 数据库（内置） | 开源，WAL 模式 |
| React 18 / Vite / TypeScript | 前端 | 开源 |
| Vditor / ECharts / KaTeX | 编辑器 / 图表 / 公式 | 开源 |
| Pandoc / cairosvg | 报告导出 / 图片转换 | 开源，离线包 |
| smolagents | AI 智能体编排 | ToolCallingAgent + 自定义模型适配 |

### 2.6 项目目录结构

本系统的代码采用前后端分离 + 分层组织，便于维护与扩展。主要目录及其职责如下：

| 目录 | 职责 |
|------|------|
| app.py | 应用入口，创建 Flask 应用、初始化数据库、注册路由、清理临时目录 |
| routes/ | 后端路由层（projects / workspace / apps / files / project_data / dashboard 等） |
| agent/ | 智能体层（core 核心 / smol_engine 引擎 / smol_tools 工具 / llm_client 模型客户端 / skill_loader） |
| storage/ | 存储层（db 数据库 / project_store 项目存储 / model_store 模型存储 / tmp_db 临时库 / agent_store） |
| tools/ | 工具层（excel 工具 / financial_analysis_tools / project_data_tools / reconcile 等确定性计算脚本） |
| config/ | 配置层（settings 全局配置） |
| data/ | 运行数据（app.db 数据库 + app-workspaces 各项目工作目录） |
| frontend/src/ | 前端源码（React 组件 / 页面 / API 封装 / hooks） |
| static/ | 前端静态资源 |
| templates/ | Flask 模板 |
| scripts/ | 构建脚本（如引用文档模板生成、测试脚本） |

### 2.7 智能体运行机制（对话工作台）

智能体是本系统的"大脑"，负责理解用户自然语言意图并编排工具调用。其核心运行机制如下：

| 环节 | 说明 |
|------|------|
| 意图理解 | 把用户一句话解析为"做什么、对哪份数据、要达到什么效果" |
| 需求分析 | 结合项目上下文与已加载的 Skill，判断需要调用哪些工具、模型 |
| 工具选择 | 从 30 类工具中选出与任务匹配的工具（读取 Excel、数据核对、建模、图表、写回等） |
| 执行调度 | 调用本地确定性脚本优先计算，需要语义能力时再调用 AI 模型补充 |
| 结果汇总 | 汇总确定性计算结果 + AI 归因结论，向用户展示 |
| 确认机制 | 涉及数据写回时，依据"人工审批 / 全自动"双工作模式决定是否需要弹窗确认 |

智能体采用"工具调用代理（ToolCalling Agent）"模式，通过 smolagents 框架完成多步推理与工具编排，并通过自定义模型适配器兼容 DeepSeek / GLM / 千问 / 内网模型等多供应商。

### 2.8 集成应用数据流（隐藏项目组件）

集成应用是架设在"隐藏项目"之上的前端 + 后端的组合，其数据流如下：

```
用户操作前端页面
   ↓
调用后端 API 端点
   ↓
操作绑定项目的数据库（按 project_id 隔离）
   ↓
调用 tools/ 下的确定性脚本计算
   ↓
结果写回 data/app-workspaces/<应用>/ 目录
   ↓
前端展示结果 / 报告
```

以"数据核对与校验"应用为例，其数据流转为：用户选择两张 Excel → 后端按匹配键读取 → 本地算法完成金额比对与五级分级 → 结果写入隐藏项目目录 → 前端展示核对报告与待审计清单 → 用户勾选差异 → 写回原 Excel 并留痕。

---

## 第三章 安装与启动

### 3.1 运行环境要求

| 项目 | 要求 |
|------|------|
| 操作系统 | Windows 10 / 11（64 位） |
| 浏览器 | Chrome / Edge，版本不低于 115 |
| 运行框架 | .NET Framework 4.0+（Windows 10/11 内置） |
| 网络 | 内网离线环境（数据全程本机） |

### 3.2 启动方式

本系统为零安装、零配置部署，有两种启动方式：

- **方式一（推荐）**：双击项目目录下的 `Finance Agent.exe`。该程序会自动启动本地服务并打开浏览器进入工作台首页。
- **方式二**：在项目目录命令行执行 `python app.py`。

启动后，系统会自动选择 8080 至 8085 之间的空闲端口，并自动打开浏览器进入"工作台总览"首页。若端口被占用，系统会自动尝试下一个端口。

### 3.3 首次使用前必做：配置 AI 模型

> 重要：本系统本身能离线运行，但"AI 问答 / 智能分析 / 报告撰写"需要先配置模型（详见第五章）。如不配置，部分"AI 自动"功能会降级为确定性功能（仍可用，仅少了 AI 归因/建议）。

### 3.4 部署形态说明

本系统采用**单机离线部署**方式，即软件、数据、AI 能力均可在用户本地电脑上运行，无需连接外部服务器。这种部署形态带来以下特点：

| 特点 | 说明 |
|------|------|
| 零安装 | 免去繁琐的依赖安装，可直接双击程序启动 |
| 离线可用 | 数据全程本机保存，不依赖外网 |
| 数据私密 | 敏感财务数据不出本机，降低泄露风险 |
| 即开即用 | 启动后自动选择端口并打开浏览器，无需手动配置 |

> 说明：软件的"数据核对、建模、图表等确定性功能"完全离线可用；只有"AI 归因、AI 报告"依赖已配置的模型。若配置了内网模型，则模型调用也不出内网，实现完全离线闭环。

### 3.5 启动后的界面载入

系统启动后会自动打开浏览器并进入"工作台总览"首页。页面顶部会显示服务运行状态（绿色"运行中"表示正常）。如果服务未能启动或页面无法打开，可检查：

1. 端口是否被其他程序占用（系统会自动尝试 8080-8085 的可用端口）。
2. 防火墙是否拦截（本机运行通常无碍）。
3. 用命令行 ${python app.py}$ 启动可看到详细的日志输出，便于排错。

---

## 第四章 界面总览

### 4.1 左侧导航栏

左侧导航栏是全局入口，分为四块：顶部导航、项目列表、集成应用。

| 入口 | 作用 |
|------|------|
| 工作台总览 | 首页统计 + 最近项目 + 应用入口 |
| AI 功能配置 | 配置模型供应商、智能体、Skill |
| 项目列表 | 显示所有项目（蓝色文件夹图标），点击进入对话工作台 |
| 集成应用 | 两款固定流程的向导应用 |

### 4.2 工作台总览（首页）

进入软件默认看到的是总览页，让你一眼掌握系统状态。

**顶部状态**：运行中 / 服务离线（标红即异常）、当前时间 + 最后刷新、刷新按钮。

**五个统计卡**（可点击跳转）：

| 统计卡 | 含义 | 点击 |
|--------|------|------|
| 可调用模型 | 可用模型数 + 默认模型 | 跳 AI 配置 |
| 项目 | 项目总数 + 应用数 | 跳对话工作台 |
| 应用 | 集成应用数 | 定位下方"应用入口" |
| 今日使用时长 | 今天累计使用时间 | — |
| 今日运行次数 | 今日任务成功/失败数 | — |

**分析图表**：7 日使用时长趋势（近 7 天每天使用时长折线图）、活跃时段分布（一天各时段使用热度柱状图）。

**最近项目 & 应用入口**：最近使用的项目（点击进入对话）、两款集成应用的快捷卡片。

### 4.3 对话工作台界面

点击任一项目名即进入该项目的工作台。界面主要分为三个区域：

| 区域 | 位置 | 用途 |
|------|------|------|
| 对话主区 | 页面中央 | 展示你与 AI 的问答消息流，支持滚动查看 |
| 输入区工具行 | 页面底部 | 模型选择、工作模式、项目目录、数据入口 |
| 输入框 | 最底部 | 输入任务描述（支持 @ 引用文件） |

**对话主区**：消息按时间顺序自上而下排列，用户消息在右侧，AI 回复在左侧。AI 回复会长文本显示，涉及表格、图表时也会以图文形式呈现。

**输入框**：在输入框中输入文字，按 Enter 发送、Shift+Enter 换行。输入 `@` 时会在右侧弹出引用面板，可快速插入项目文件、数据库表等。

### 4.4 顶部/全局导航

页面的顶部导航栏提供全局入口，部分关键按钮说明如下：

| 入口/按钮 | 作用 |
|-----------|------|
| 工作台总览 | 返回首页 |
| AI 功能配置 | 进入模型/智能体/Skill 配置 |
| 项目列表 | 显示所有项目与集成应用 |
| 运行状态 | 顶部实时显示服务是否正常 |

### 4.5 项目列表与集成应用入口

左侧导航栏的"项目列表"会列出所有已创建的项目（蓝色文件夹图标）以及两个固定的集成应用（数据核对与校验、财务建模与分析）。用户可：

- 点击项目名进入该项目的对话工作台；
- 点击集成应用进入对应业务流程向导；
- 通过项目操作菜单（⋮）对项目进行重命名、创建副本、删除等管理。

### 4.6 界面操作提示

系统界面整体以图形化操作（点击、下拉、勾选）为主，绝大多数功能不需要命令行或编程知识。关键操作路径为：**进入对应页面 → 按向导或对话交互 → 系统自动执行 → 查看结果**。对于不熟悉的操作，页面顶部或关键位置通常会显示提示文案，如"按向导配置两表核对"、"你的分析目标是什么？"等，引导用户完成流程。
"""

# 第二半部分（第五～第八章 + 附录）
DOC_MD_2 = r"""
---

## 第五章 AI 功能配置

> 用途：告诉软件"用哪个 AI 模型、怎么调"。不配置 = 用不了 AI 功能。

### 5.1 三个标签页

| Tab | 作用 | 是否必配 |
|-----|------|---------|
| 模型配置 | 管理"供应商 + 模型" | 必配 |
| 智能体配置 | 查看智能体执行模式/参数 | 只读 |
| Skill 配置 | 查看已加载技能模块 | 只读 |

### 5.2 模型配置：添加供应商（首次必做）

1. 右上角点击加号（新增供应商）。
2. 弹窗填写：名称（任意，如"内网模型"）、API 地址（模型服务网址）、API Key（密钥）、描述（可选）。
3. 点击"测试连接"确认能通。
4. 点击"保存"。

> 易错点：API 地址填的是网址，API Key 填的是密钥，两者不要混填。

### 5.3 供应商下的模型

供应商可挂多个模型（如 deepseek-v4-flash、glm、qwen）。每张模型卡右侧有图标：

| 图标 | 作用 |
|------|------|
| ⭐ | 设为默认模型 |
| ✏️ | 编辑模型（名称/图标/类型/能力/上下文窗口） |
| 🗑 | 删除模型 |

**添加模型**：供应商卡片内点"＋ 添加模型"，填名称 → 保存。

**模型字段**：类型（chat 对话 / embedding 嵌入 / reranker 排序等）、能力（对话、工具、视觉）、上下文窗口（一次处理多少文字）、设为默认。

### 5.4 智能体配置 / Skill 配置（只读）

- 智能体配置：查看智能体的执行模式、最大迭代、计划审批、上下文预算等。
- Skill 配置：查看已加载的技能（如财务建模 Skill）及其步骤。

这两页主要给实施人员看，日常不用改动。

### 5.5 模型字段详解

添加或编辑模型时，需要填写以下字段：

| 字段 | 说明 |
|------|------|
| 名称 | 模型显示名称，如"deepseek-v4-flash" |
| 类型 | chat（对话）/ embedding（嵌入）/ reranker（排序）等 |
| 能力 | 对话、工具调用、视觉识别等能力标记 |
| 上下文窗口 | 模型一次能处理的最大文字量 |
| 设为默认 | 勾选后该模型成为系统默认使用的模型 |

### 5.6 配置建议

为保证最佳使用效果，建议：

1. **首选搭配**：配置一个具备"对话 + 工具调用"能力的模型作为默认模型。
2. **测试连接**：保存前务必点击"测试连接"，确认模型服务可达、API Key 正确。
3. **供应商隔离**：不同供应商（如 DeepSeek、GLM、千问、内网模型）可同时配置，按需切换。
4. **内网模型优先**：若数据敏感，优先配置内网部署的模型，使模型调用不出内网。
5. **降级兜底**：即使不配置模型，系统的核对、建模、图表等确定性功能仍可正常使用，只是缺少 AI 归因与报告能力。

---

## 第六章 对话工作台（项目空间）

> 用途：自由地与 AI 对话，让它读你的 Excel、做分析、修数据——长尾自由任务的入口。

### 6.1 进入

左侧"项目列表"点一个项目名，即进入该项目工作台。

### 6.2 界面组成

| 区域 | 说明 |
|------|------|
| 对话主区 | 你与 AI 的消息流（居中滚动） |
| 输入区工具行 | 模型选择器 / 工作模式 / 项目目录 / 数据 |
| 输入框 | 输入任务，Enter 发送、Shift+Enter 换行 |

**空状态**会显示：

- "尝试这样开始"建议提示词（点击填充输入框）。
- "我能做什么"能力清单（项目文件/Excel分析/差异核对/图表生成/财务建模/写回）。

### 6.3 输入区工具行

| 按钮 | 作用 |
|------|------|
| 模型选择器 | 切换 AI 模型（下拉选择） |
| 工作模式 | 人工审批（改数据前问你）/ 全自动（直接执行） |
| 项目目录 | 打开右侧文件目录抽屉 |
| 数据 | 打开项目数据库 |

**工作模式（重要）**：

- 人工审批：AI 要写回数据前会先弹出确认，你没有同意不会改 Excel。
- 全自动：AI 直接执行写入（适合信任场景）。

### 6.4 项目目录抽屉（右侧）

点工具行"项目目录"打开，可管理该项目的全部文件。

**顶部工具按钮**：新建（文件/文件夹）、上传（文件/文件夹）、刷新目录、关闭抽屉。

**双击文件**：Excel / Word / PPT 直接调本机程序打开；PDF / 图片 / 文字 / Markdown 新标签页预览。

**右键文件**（更多操作）：打开 / 添加到项目数据库 / 引用（插入 @file: 到输入框）、重命名 / 复制 / 剪切 / 粘贴 / 删除。

> 注意：删除会删除系统磁盘上的真实文件，不可恢复，请谨慎。

### 6.5 怎么做一次分析

1. 左侧选好项目。
2. 点"项目目录"找到要分析的 Excel。
3. 在输入框描述需求（可按 @ 引用文件），如"帮我核对这份 Excel 的金额"。
4. 发送 → AI 读取、分析、展示结果。
5. 需要改数据时 → 依据工作模式确认（人工审批会弹窗）。

### 6.6 对话示例场景

以下列举几种典型对话交互，帮助理解如何使用：

**场景一：核对两份表**
用户："帮我核对《季度成绩表》和《年度汇总表》的金额，找出差异。" → AI 读取两份 Excel，按匹配键比对，输出差异清单并分级。

**场景二：分析数据**
用户："分析一下这份销售表最近三个月的增长趋势。" → AI 调用建模与图表工具，生成趋势图与结论。

**场景三：生成图表**
用户："把这份数据的各月份占比做成饼图。" → AI 调用图表工具，生成饼图并嵌入对话展示。

**场景四：修改数据**
用户："把 A 客户那几条金额改成正确值。" → AI 依据工作模式（人工审批会先确认），修改并留痕。

### 6.7 更多对话技巧

| 技巧 | 说明 |
|------|------|
| 使用 @ 引用 | 输入 @ 可快速插入项目文件/目录/数据库，避免手打文件名 |
| 描述清晰 | 用"做什么 + 对哪份数据 + 达到什么效果"的结构描述需求 |
| 分步提问 | 复杂任务分解为多个小问题，结果更准确 |
| 关注结果 | AI 输出后可追问"为什么"获取归因，或"导出"生成报告 |

### 6.8 项目创建与管理

**创建新项目**：在工作台总览或项目列表点击"新建项目"，输入项目名称即可创建。每个项目是一个独立的工作容器，拥有自己的文件、对话与数据库。

**项目管理**（通过项目操作菜单 ⋮）：
- 重命名：修改项目名称。
- 创建副本：复制项目及其数据。
- 删除：删除项目及其关联数据（谨慎操作，会删除真实数据）。

### 6.9 智能体能力清单

对话工作台的智能体具备以下能力，可通过自然语言触发：

| 能力 | 说明 | 示例指令 |
|------|------|---------|
| 读取文件 | 读取项目内 Excel / 文本文件 | "读取这份 Excel" |
| Excel 分析 | 对 Excel 做汇总、筛选、统计 | "统计各销售员回款" |
| 数据核对 | 分析两份表的差异 | "对比这两份表的金额" |
| 图表生成 | 生成趋势/对比/占比图 | "做成趋势图" |
| 财务建模 | 调用财务模型计算 | "测算成本结构" |
| 数据写回 | 修改 Excel 并留痕 | "把这几条改掉" |
| 报告生成 | 生成分析报告 | "出份分析报告" |

### 6.10 上下文与记忆

智能体具备一定的上下文管理能力：
- **会话记忆**：同一对话内能记住前文，用户可连续追问。
- **项目上下文**：自动识别当前项目的文件、数据库结构。
- **上下文管理**：超长对话自动压缩，避免超出模型上下文窗口。

---

## 第七章 数据核对与校验（集成应用一）

> 用途：把两份 Excel（如"依据表"与"分表"）做金额核对，找出差异、审计确认、自动修正。适合做账、对账场景。

### 7.1 进入

左侧"集成应用"→ 数据核对与校验。

### 7.2 顶部共享栏

- 标题：数据核对与校验。
- 副标题：按向导配置两表核对，AI 自动完成匹配、分级、归因与写回。
- 模型选择器：右上角（选择本次 AI 基于哪个模型）。
- 应用专用 AI 卡：可点击展开，查看"AI 职责与约束"。

### 7.3 核心：六步向导（左侧竖排，可回退）

| 步骤 | 你要做什么 |
|------|-----------|
| ① 数据文件 | 选依据表（基准）+ 分表（待核对） |
| ② 匹配键 | 设两表共有的唯一标识列（一级必填，可加二级/三级） |
| ③ 核对变量 | 选要核对金额的列（分表金额列 + 依据表金额列），带 AI 推荐 |
| ④ 容差&策略 | 设容差（元）+ 匹配不上如何处理 + 是否自动写回 A 类 |
| ⑤ 校验 | 点"开始校验"，出结果（核对报告 + 待审计清单） |
| ⑥ 修改 | 勾选差异 → 确认写回 → 输出文件 |

### 7.4 六步详解

#### ① 数据文件

标题"选择数据文件"，两个下拉：依据表（基准）、分表（待核对）。右上角"项目数据库"按钮：上传/删除/刷新 Excel 数据。

**项目数据库抽屉（重要）**：点"项目数据库"从右侧滑出，支持上传 Excel（多文件）、表头锚定确认（上传时确认"数据从第几行开始"）、索引查看 / 删除 / 刷新。

#### ② 匹配键

选择两表共有的"唯一标识"列，用来把两边数据对上（如合同编号）。支持三级匹配键（合同编号 → 人员 → 客户，一级必填）。

#### ③ 核对变量

选择要核对金额的列（分表金额列 + 依据表金额列），带 AI 推荐。

#### ④ 容差与策略

| 配置项 | 说明 |
|--------|------|
| 容差（元） | 金额相差多少内算"匹配"（快捷值 0 / 0.01 / 0.1 / 1 / 5） |
| 匹配不上怎么办 | A. 列表清单供人工复核 / B. 按（人员+客户）降级 / C. 全部标记无法匹配 / D. 自定义 |
| A 类差异自动写回 | 开关（开启则 A 类差异确认后自动修正） |

#### ⑤ 校验结果页

三个区块：

1. 核对报告：AI 生成的核对结果报告（Markdown）。
2. 待审计清单：未匹配/有疑问的记录，逐个处理。
3. 历史运行：之前的核对记录（可查看/一键重跑）。

**待审计清单处理**：上方有分组 tab（全部 / 总表独有 / 分表独有 / 一对多模糊）。每条记录类型、关键值（匹配键）、原因，右侧下拉选"我的决策"（补录/忽略/确认对应关系）。底部"提交决策 → 下一轮核对"，可多轮审计直至清零。

#### ⑥ 确认修改（写回）

进入后显示差异明细表（每页 200 条）：每行勾选框、级别（OK / A1 漏填 / A2 误差 / B 待确认 / C 无法匹配）、合同编号、金额、差异、AI 归因、AI 建议。A1/A2 默认勾选；B 需人工勾选；C 不可勾选（不可修正）。

**写回**：点"确认修改（N 条）"自动把勾选的差异精确写回原 Excel（只改勾选的单元格，其它格式/公式 100% 保留）。写回前原文件自动备份（.bak），可恢复。完成后出现"输出文件"：每条文件可打开 / 下载导出。

### 7.5 五级差异分级

核对结果自动分为五级：

| 级别 | 含义 | 处理 |
|------|------|------|
| OK | 匹配一致 | 无需处理 |
| A1 | 漏填缺失 | 默认勾选修正 |
| A2 | 小额误差 | 默认勾选修正 |
| B | 待确认 | 需人工勾选 |
| C | 无法匹配 | 不可勾选（不可修正） |

未匹配记录可升级为待审计项，人工决策后进入下一轮核对，直至清零。形成"机器分级 + 人工审计 + 闭环迭代"，既高效又保留财务专业判断。

### 7.6 核对核心流程（数据流）

数据核对应用的核心流程可分为以下阶段：

| 阶段 | 说明 |
|------|------|
| 数据导入 | 选择两张 Excel（依据表 / 分表），系统读取并物化为内部数据表 |
| 表头定位 | 智能识别每张表的表头起始行，引导用户确认数据从第几行开始 |
| 匹配键配置 | 设定两表共有的唯一标识列（支持多级匹配键） |
| 金额比对 | 按匹配键逐条比对金额，计算差异，纳入容差判定 |
| 差异分级 | 依据差异大小与匹配情况，自动分为 OK / A1 / A2 / B / C 五级 |
| 归因分析 | AI 对差异给出可能原因（漏填、误差、录入错误等） |
| 人工审计 | 对待确认项逐条决策（补录 / 忽略 / 确认对应关系） |
| 写回留痕 | 只改勾选单元格，备份原文件，记录字段级修改日志 |

### 7.7 多级匹配机制

对于"一对多"或"多层级"的数据，系统支持**多级匹配键**。例如，一级匹配键为"合同编号"，若两表合同编号对不上，可再按"人员 + 客户"等二级、三级键进行降级匹配。这大大提高了复杂数据的核对效率，降低人为遗漏。

### 7.8 容差与匹配策略

在步骤④中，用户可设置"容差"（金额相差多少以内算匹配），以及"匹配不上怎么办"的处理策略：

| 策略 | 说明 |
|------|------|
| 列表清单供人工复核 | 未匹配记录进入待审计清单，由人工逐条处理 |
| 按二级键降级 | 一级匹配失败时，按人员、客户等二级键继续尝试匹配 |
| 全部标记无法匹配 | 未匹配记录标记为 C 级，不可自动修正 |
| 自定义 | 由用户自定义匹配规则 |

---

## 第八章 财务建模与分析（集成应用二）

> 用途：把多份 Excel 合并，做财务建模、图表、趋势分析，并自动生成可导出 Word 的分析报告。适合不懂建模但想要分析结论的业务人员。

### 8.1 进入

左侧"集成应用"→ 财务建模与分析。

### 8.2 核心：四步向导 + 历史记录

左侧竖排：① 诉求与数据 → ② 宏观建模与映射 → ③ 图表风格 → ④ AI 撰写报告，分隔线下方是历史记录（第 5 项）。

### 8.3 步骤① 诉求与数据

- 标题"你的分析目标是什么？"
- 诉求输入框：一句话描述目标（如"分析 2025 年各月回款提成的季度趋势与波动原因"）。
- 数据文件（项目数据库 · 已导入 N 个）：勾选参与分析的 Excel（可多选，自动合并建模）。
- "管理数据（上传/删除）"按钮：打开项目数据库抽屉上传新 Excel。
- 点"下一步"进入建模。

### 8.4 步骤② 宏观建模与映射

含两个子 tab：

- **① 建模方向**：AI 根据目标推荐分析模型（默认勾选，可取消）；下方"模型库"（A-G 分类，32 个）可手动补充；选至少 1 个才能下一步。
- **② 变量映射**：自动把模型所需变量对应到你的列（低置信可手动改下拉）；支持派生公式、数据预处理。

### 8.5 步骤③ 图表风格

- 复杂度（简单/中等/复杂）+ 偏好类型（趋势/对比/占比等，多选）+ 配色（蓝橙/蓝绿等）。
- AI 推荐图表（默认加入清单，可移除）。
- 图表库可手动勾选补充。
- 确认清单（N 张，可调变体/配色/顺序）。
- 点"下一步（AI 撰写报告）"。

### 8.6 步骤④ AI 撰写报告

含两个子 tab：

- **① 确定性执行**：自动并行计算模型 → 显示"模型计算结果"（成功/失败）+ "图表（N 张）"（所见即所得）→ 底部"下一步生成"。
- **② AI 撰写报告**：点击后自动生成报告（进度条"AI 正在撰写报告…"）→ 完成后内嵌预览。

**报告预览区（重要）**：标题"报告预览" + 徽章"AI 报告"（或"确定性降级"）。报告直接预览，可编辑（直接改文字）、保存、导出 Word / PDF（内置功能，含图表、三线表排版）。

### 8.7 历史记录

点"历史记录"进入独立页：显示所有已生成分析报告（文件名 + 生成时间 + 分析诉求）。每条操作：打开（新标签页预览原报告）/ 删除。顶部"刷新"按钮。空态："暂无历史报告"。

### 8.8 核心技术能力

#### 多文件合并引擎

跨文件按同构/共同键自动合并，统一建模，能够实现多表统一分析。

#### 32 个财务模型

A-G 七大类（报表/预算/成本/绩效/资本/营运/统计），AI 按诉求自动推荐，业务人员无需建模知识。

#### 报告产出

核心观点 / 分析基础 / 分析过程 / 结论建议结构 + 专业排版（含图表、三线表），支持 Markdown 在线编辑渲染与导出（支持 Word、PDF、HTML 多种格式）。

#### 历史记录沉淀

所有报告留存，可预览、删除。

### 8.9 32 个财务模型分类

系统内置 32 个财务模型，按 A-G 七大类组织，覆盖财务分析的常见需求：

| 分类 | 说明 | 典型模型 |
|------|------|---------|
| A 报表分析 | 财务报表结构、比率分析 | 资产负债表结构、利润表趋势 |
| B 预算分析 | 预算执行、偏差分析 | 预算达成率、预算偏差 |
| C 成本分析 | 成本结构、成本变动 | 成本构成、单位成本 |
| D 绩效分析 | 业绩达成、KPI 指标 | 销售达成率、回款率 |
| E 资本运营 | 资金周转、资本结构 | 应收账款周转、净现值 |
| F 营运分析 | 营运效率、经营指标 | 存货周转、营运效率 |
| G 统计分析 | 趋势、相关、回归 | 时间趋势、相关分析 |

### 8.10 图表库与风格

系统内置 24 张图表模板，按复杂度分 L1 / L2 / L3 三档：

| 档位 | 复杂度 | 适用场景 |
|------|--------|---------|
| L1 | 简单 | 基础趋势、占比 |
| L2 | 中等 | 对比、构成、时间序列 |
| L3 | 复杂 | 多维度、多系列分析 |

用户可在步骤③中选择复杂度、偏好类型（趋势 / 对比 / 占比等）、配色方案（蓝橙、蓝绿等），系统自动匹配或由 AI 推荐合适的图表。

### 8.11 报告结构说明

财务建模分析最终产出的报告包含以下结构：

| 结构 | 说明 |
|------|------|
| 核心观点 | 一句话概括分析的核心结论 |
| 分析基础 | 数据来源、范围、口径说明 |
| 分析过程 | 关键计算、图表、模型说明 |
| 结论建议 | 针对核心观点给出业务建议 |

报告由 AI 或确定性引擎自动生成，采用三线表、图表与专业排版，支持在线编辑与导出（Word / PDF / HTML）。
"""

# 第三半部分（第九章～附录）
DOC_MD_3 = r"""
---

## 第九章 关键技术实现

### 9.1 数据平面

本系统把 Excel 转成内部数据库表（data_* 表），AI 在这上面分析，不直接改 Excel 源文件。数据上传后物化为 SQLite 表，分析只读；修改在独立临时库快照内并留痕，主库零污染。

### 9.2 临时库快照与隔离

核对/写回在独立临时库快照执行，主库零污染。临时运行目录超 24 小时自动清理，活动 run 受保护。这样确保：

- 数据按 project_id 隔离，互不污染。
- 修改可回滚，可追溯。
- 运行垃圾不堆积。

### 9.3 字段级修改日志

系统记录字段级修改日志（列 + 改前/后值），实现全链路留痕。每次数据修正都可回溯执行人、时间、前后值。报告留痕：每份报告含来源 run_id，可回溯执行。

### 9.4 确定性计算 + AI 辅助

核对/分级/建模/图表优先调用本地 Python 脚本（纯算法，0 Token）；AI 只做引导/归因/报告。这样：

| 场景 | Token 消耗 |
|------|-----------|
| 用户指令理解 / 建模引导 | 低（结构化摘要，非全量数据） |
| 差异判断（核对） | 0 Token（本地确定性分级） |
| 图表生成 | 0 Token（本地 ECharts/Python） |
| 报告撰写 | 中（仅传结果摘要 + 图表清单） |

### 9.5 文件锁检测

写回前检测文件占用。若 Excel 被其他程序打开，系统提示关闭重试，不强行写入。

### 9.6 表头智能定位

上传 Excel 时自动识别表头行，引导用户确认"数据从第几行开始"，确保数据解析正确。

### 9.7 多文件合并引擎

财务建模的"多文件合并"是跨文件、跨表自动合并。其实现机制为：

1. **表结构识别**：识别每个 Excel 文件的表头、字段名、数据类型。
2. **同构判断**：判断多个文件是否为同构（字段一致）或需按共同键关联。
3. **统一合并**：将多个文件按相同字段或共同键合并为一张内部数据表。
4. **统一建模**：在合并后的数据表上执行财务模型计算、图表生成。

这样，用户只需勾选想要分析的多份 Excel，系统即可自动把它们合并为统一的数据集进行分析，无需手工拼接。

### 9.8 报告导出引擎

报告的导出（Word / PDF / HTML）依赖 Pandoc 工具链与自定义模板：

| 环节 | 说明 |
|------|------|
| 内容生成 | 报告内容以 Markdown 形式生成 |
| 图片处理 | 图表（SVG）通过 cairosvg 转为 PNG，保证 Word 兼容 |
| 模板套用 | 使用 Pandoc 引用模板（三线表样式）渲染 |
| 格式输出 | 输出为 Word / PDF / HTML |

### 9.9 变量索引机制

为了在调用 AI 模型时保护数据隐私，系统采用"变量索引"机制——只把数据表的**结构信息**（表名、列名、类型、少量样例）提供给 AI，而不传输完整数据。这样即使模型在线，也不会泄露敏感财务数据。

---

## 第十章 数据安全与合规

### 10.1 数据隔离

- **项目空间隔离**：每个业务/应用独立隐藏项目（`__app_recon__` / `__app_finmod__`）+ 独立目录，数据按 project_id 隔离。
- **数据分层**：上传 Excel → 物化为 SQLite 表（分析只读）；修改在独立临时库快照内并留痕，主库零污染。

### 10.2 访问控制与追溯

- **本地单机**：运行在用户电脑，无多用户权限管理。
- **操作追溯**：字段级修改日志 + 运行记录，可回溯执行人/时间/前后值。
- **双工作模式**：人工审批确保写回前征求确认。

### 10.3 敏感信息与数据安全

- **数据全本地**：data/app.db + 产物目录全部本机保存，离线运行不外传。
- **服务绑定 127.0.0.1**：外网不可达。
- **AI 只输入数据库变量索引**（读列名/类型，不读全量数据）——即使联网调模型，也不泄露完整敏感数据。
- **模型可选内网本地模型**（数据全程不出域）或外网资源（仅传结构化摘要）。

### 10.4 操作审计日志

- **字段级修改日志**（列 + 前后值）。
- **运行记录**（会话时长 + 本地操作日志 + 活动 run 注册表）。
- **报告留痕**（每份报告含来源 run_id，可回溯执行）。

### 10.5 权限与分工

本系统定位为单机单用户工作台，不涉及多用户权限管理。但通过以下机制实现"职责分离"：

| 机制 | 说明 |
|------|------|
| 机器分级 + 人工审计 | 机器负责确定性计算，用户负责业务判断 |
| 人工审批模式 | 涉及写回时需用户确认，防止误操作 |
| 报告留痕 | 每份报告记录来源，可回溯分析依据 |

### 10.6 数据生命周期

| 阶段 | 处理 |
|------|------|
| 导入 | 上传 Excel → 物化为 SQLite 内部表（分析只读） |
| 处理 | 在独立临时库快照内执行，主库零污染 |
| 修改 | 只改勾选单元格，字段级日志留痕 |
| 导出 | 报告导出 Word / PDF，数据不随报告外传 |
| 清理 | 临时运行目录超 24 小时自动清理，活动 run 受保护 |

---

## 第十一章 异常处理与维护

### 11.1 异常处理机制

| 异常场景 | 检测机制 | 处理措施 |
|---------|---------|---------|
| Excel 被其他程序打开 | 文件锁检测 | 提示关闭重试，不强行写入 |
| 网络中断 / 模型不可用 | 连接检测 | 核对/建模/图表确定性降级仍可用 |
| 数据格式异常 | 表头智能定位 | 引导用户确认表头/锚点 |
| 重复执行 | 文件哈希去重 + 幂等 | 复用已有导入 |
| 系统崩溃 | 临时库快照 + 活动 run 注册表 | cleanup_stale 兜底清理 |
| 运行垃圾堆积 | cleanup_stale（mtime 超 24h） | 自动清理，活动 run 保护 |

### 11.2 扩展与维护

- **新增对账/分析规则**：Skill 声明式，新增业务规则直接加/改 Skill，不改核心代码。模型库（32 个）、图表库（24 类）通过 references/ 配置扩展。
- **新增集成应用**：基于隐藏项目组件，新增一个应用只需加前端页 + 加后端端点 + 建隐藏项目，完全复用主工作台基础设施，成本极低。
- **模型替换或升级**：支持 DeepSeek / GLM / 千问 / 内网模型多供应商，随时切换/测试。

### 11.3 性能与资源

| 指标 | 说明 |
|------|------|
| 核对规模 | 单次可核对数万条记录（取决于内存） |
| 计算速度 | 确定性核对/建模为秒级，毫秒级完成比对 |
| Token 消耗 | 确定性计算 0 Token，仅 AI 归因/报告消耗少量 |
| 运行稳定性 | WAL 模式数据库 + 临时库快照，崩溃可恢复 |

---

## 第十二章 常见问题与提示

1. **提示"AI 功能未配置/模型不可用"** → 到第五章配好模型供应商+模型。
2. **Excel 传上去后看不到数据** → 上传时注意表头行确认（数据从第几行开始）。
3. **报告里图表"裂图"** → 属于预览旧缓存，Ctrl+F5 刷新。
4. **导出 Word 图片看不到** → 需服务端已装 cairosvg（离线包已含）+ 重启服务。
5. **想换 AI 模型** → 对话工作台底部 / 应用页面的模型选择器切换。
6. **端口被占用** → Finance Agent.exe 自动选 8080-8085 空闲端口。
7. **数据安全** → 数据保存在本机 data/，离线运行不外传。
8. **写回会覆盖原文件吗？** → 不会直接覆盖，先自动备份 .bak，原格式/公式 100% 保留。
9. **"人工审批"和"全自动"区别** → 人工审批：写回前弹窗确认；全自动：直接执行。
10. **能同时用多个模型吗？** → 可以，多个供应商/模型并存，通过模型选择器切换。
11. **数据存在哪里？** → 本机 data/ 目录（app.db + app-workspaces），离线运行不外传。
12. **怎么确认数据核对准不准？** → 采用五级差异分级 + 人工审计闭环，机器分级、人工复核，可追溯。
13. **报告里的图表能不能改？** → 生成图表前可调整变体/配色/顺序；报告生成后可在线编辑。
14. **新增一个财务规则要改代码吗？** → 不用，通过 Skill / 模型库 / 图表库配置扩展。
15. **这个系统能跑多大数据？** → 数万条记录的核对/建模均可，具体取决于本机内存。

---

## 第十三章 分场景操作指南

### 13.1 场景一：季度销售回款提成核对

**背景**：财务每季度需将季度分表与年度汇总逐条比对，核对合同编号、销售人员、客户单位及金额。

**操作步骤**：
1. 启动系统，登录工作台。
2. 左侧"集成应用"→"数据核对与校验"。
3. 步骤①：选择"依据表"（年度汇总）与"分表"（季度分表）。
4. 步骤②：设置匹配键（如合同编号，可加人员、客户为二级键）。
5. 步骤③：选择要核对的金额列（分表金额列 + 依据表金额列）。
6. 步骤④：设置容差（如 0.01 元），选择"匹配不上"处理策略。
7. 步骤⑤：点击"开始校验"，生成核对报告与待审计清单。
8. 步骤⑥：勾选差异（A1/A2 默认勾选），点"确认修改"写回原表。
9. 完成，输出核对后的 Excel 文件。

### 13.2 场景二：多文件财务趋势分析

**背景**：用户想分析 2025 年各月回款提成的季度趋势与波动原因，但数据分散在多份 Excel。

**操作步骤**：
1. 左侧"集成应用"→"财务建模与分析"。
2. 步骤①：输入分析诉求（一句话描述目标）。
3. 步骤①：勾选参与分析的多份 Excel（自动合并建模），可先上传。
4. 步骤②：确认 AI 推荐的建模方向，映射变量（低置信可手动调整）。
5. 步骤③：设置图表复杂度、偏好类型、配色，AI 推荐图表清单。
6. 步骤④：点"下一步生成"→ AI 撰写报告 → 预览报告。
7. 导出 Word / PDF，或保存到历史记录。

### 13.3 场景三：对话式临时分析

**背景**：用户有一份 Excel，想临时问问里面的数据情况，不想走完整向导。

**操作步骤**：
1. 左侧"项目列表"进入一个项目对话工作台。
2. 点"项目目录"找到要分析的 Excel。
3. 输入框描述需求，如"分析这份表各销售员的回款占比"。
4. 发送 → AI 读取分析并展示结果。
5. 需要修改数据时，根据工作模式确认后写回。

### 13.4 场景四：配置内网 AI 模型

**背景**：数据敏感，希望 AI 调用不出内网。

**操作步骤**：
1. 左侧"AI 功能配置"→"模型配置"。
2. 点右上角"＋"，添加供应商，填写内网模型服务地址与 API Key。
3. 点击"测试连接"确认可用。
4. 保存，并勾选该模型为默认模型。
5. 之后各页面模型选择器即可切换使用。

### 13.5 场景五：项目空间管理

**背景**：用户想把不同类型的工作分散管理，避免数据混在一起。

**操作步骤**：
1. 工作台总览 / 项目列表 → 点"新建项目"，创建独立工作空间。
2. 进入项目对话工作台，上传该项目的 Excel 文件。
3. 通过"项目目录"管理文件，通过"数据"查看项目数据库。
4. 需要时对项目做重命名、创建副本、删除等管理。

---

## 第十四章 集成应用详解

### 14.1 集成应用是什么

集成应用是把**高频、固定的业务流程固化为向导式应用**，业务人员按步骤操作即可产出专业结果，无需建模/编程知识。它与对话工作台本质上是同一套引擎的两种形态：

| 形态 | 特点 | 适用 |
|------|------|------|
| 对话工作台 | 自由、临时、长尾任务 | 灵活分析、临时查询 |
| 集成应用 | 高固定流程、向导式 | 高频、标准化的业务 |

### 14.2 两款集成应用对比

| 对比项 | 数据核对与校验 | 财务建模与分析 |
|--------|--------------|--------------|
| 定位 | 两表金额核对 | 多表合并建模分析 |
| 核心功能 | 匹配、分级、写回 | 建模、图表、报告 |
| 业务场景 | 对账、做账 | 趋势、分析 |
| 产出 | 核对报告 + 修正后 Excel | 分析报告 + 图表 |

### 14.3 隐藏项目组件原理

每个集成应用本质上是架设在一个"隐藏项目"之上的前端 + 后端组合。隐藏项目是 projects 表中一条以 `__app_*` 前缀命名的记录（如 `__app_recon__`、`__app_finmod__`），它复用了主工作台的项目、存储、文件、前端能力。

这种设计的好处是：
- 集成应用不需要从零搭建自己的存储、文件系统、前端组件。
- 数据按 project_id 与其它项目隔离，互不污染。
- 各应用的数据沉淀在同一 app.db 中，统一备份、统一日志。

### 14.4 如何扩展新的集成应用

在隐藏项目组件架构下，新增一个集成应用的成本很低，只需三步：
1. 新建一个隐藏项目记录（`__app_xxx__`）。
2. 添加对应的前端页面并挂载到应用导航。
3. 添加配套的后端接口端点。

这三步完成后，即可复用主工作台的存储、文件、前端组件等基础设施，快速上线新的业务流程应用。

---

## 第十五章 数据与存储说明

### 15.1 数据存储架构

本系统的数据采用"统一数据库 + 按项目隔离"的存储架构：

| 存储对象 | 位置 | 说明 |
|---------|------|------|
| 系统数据库 | data/app.db | 存放项目、模型、会话、运行记录等 |
| 项目工作目录 | data/app-workspaces/<项目>/ | 每个项目的文件、产物、报告 |
| 数据表 | 数据库内 data_* 表 | 上传的 Excel 物化后的内部表 |
| 临时库快照 | 独立临时目录 | 核对/写回时的隔离快照 |

### 15.2 数据库说明

系统使用 SQLite 数据库（WAL 模式），主要表包括：

| 表 | 作用 |
|----|------|
| projects | 项目信息（含隐藏项目 __app_*） |
| models | AI 模型配置 |
| sessions | 会话记录 |
| runs | 任务运行记录 |
| agent_store | 智能体状态 |
| token_store | Token 使用记录 |

### 15.3 数据备份与恢复建议

虽然系统本身不做自动备份，但建议日常注意：
1. 关键数据文件（data/app.db）定期复制备份。
2. 项目产物目录（data/app-workspaces）可整体备份。
3. 写回前系统会自动生成 .bak 备份，可恢复原文件。

---

## 附录 A 菜单 / 快捷键速查

### A.1 全局图标含义

| 图标 | 含义 |
|------|------|
| 工作台总览 | 首页 |
| 设置 | AI 功能配置 |
| 蓝色文件夹 | 项目 / 项目目录 |
| AppWindow | 集成应用（数据核对 / 财务建模） |
| ⋮ | 项目操作菜单（重命名/副本/删除） |
| ⭐ | 设为默认模型 |
| ✏️ | 编辑 |
| 🗑 / 删除 | 删除 |
| ↻ / 刷新 | 刷新 |
| ＋ | 新建 / 新增 |

### A.2 常用快捷键

| 快捷键 | 作用 |
|--------|------|
| Enter | 发送消息（对话） |
| Shift+Enter | 换行 |
| @ | 引用项目文件/目录/数据库 |
| F2 | 项目/文件重命名 |
| Ctrl+F5 | 强制刷新页面（清缓存） |

### A.3 对话工作台顶部按钮

| 按钮 | 作用 |
|------|------|
| 模型选择器 | 切换 AI 模型 |
| 人工审批 / 全自动 | 数据写回前是否需确认 |
| 项目目录 | 打开文件目录（管理/引用文件） |
| 数据 | 打开项目数据库 |

### A.4 数据核对应用界面元素

| 元素 | 作用 |
|------|------|
| 依据表 / 分表 | 选择核对的两张 Excel（基准表 / 待核对表） |
| 匹配键选择 | 设置两表共有的唯一标识列 |
| 核对变量 | 选择要核对金额的列 |
| 容差设置 | 设定金额匹配容差 |
| 开始校验 | 启动核对计算 |
| 核对报告 | 展示核对结果 |
| 待审计清单 | 逐条处理未匹配/有疑问记录 |
| 确认修改 | 勾选差异后写回原 Excel |
| 输出文件 | 查看/下载核对后文件 |

### A.5 财务建模应用界面元素

| 元素 | 作用 |
|------|------|
| 分析目标输入 | 一句话描述分析诉求 |
| 数据文件勾选 | 选择参与分析的多份 Excel |
| 建模方向 | AI 推荐或手动选择的财务模型 |
| 变量映射 | 将模型变量对应到数据列 |
| 图表风格 | 设置复杂度、类型、配色 |
| 图表清单 | 确认要生成的图表 |
| 模型计算结果 | 展示确定性计算的结果 |
| AI 撰写报告 | 自动生成分析报告 |
| 报告预览 | 内嵌预览 / 编辑报告 |
| 历史记录 | 查看/删除历史报告 |

---

## 附录 B 词汇表

| 术语 | 通俗解释 |
|------|---------|
| 项目 | 一次工作任务的容器，装文件、对话、数据库。 |
| 模型 / 供应商 | 供应商=提供 AI 服务的地方；模型=里面的具体 AI 能力。 |
| 数据平面 | 软件把 Excel 转成内部数据库表（data_*），AI 在这上面分析，不直接改 Excel 源文件。 |
| 索引 | 数据表的结构信息（有哪些表、列、类型、样例）。 |
| 匹配键 | 两表共有的"唯一标识"列，用来把两边数据对上（如合同号）。 |
| 容差 | 金额相差多少以内算"匹配上了"（元）。 |
| 写回 | 把核对/修正结果写回原 Excel。写回前会自动备份 .bak。 |
| 人工审批 / 全自动 | 写回数据前，AI 是否要征求你同意。 |
| 隐藏项目 | 集成应用背后自动创建的项目（__app_*），不用你管，只管用应用。 |
| AI 报告 / 确定性降级 | 能连上 AI 时是"AI 报告"；连不上时软件用内置规则生成"确定性"报告（仍可用）。 |

---

## 附录 C 软件模块清单

本系统按功能可划分为以下模块，各模块职责清晰、边界明确：

| 模块 | 对应目录/文件 | 职责 |
|------|--------------|------|
| 应用入口 | app.py | 创建 Flask 应用、初始化数据库、注册路由、启动服务 |
| 智能体模块 | agent/ | 意图理解、工具编排、模型调用、SKILL 加载 |
| 数据存储模块 | storage/ | 数据库连接、项目/模型/会话/运行记录存储 |
| 工具计算模块 | tools/ | Excel 读写、数据核对、财务建模、图表生成 |
| 路由接口模块 | routes/ | 提供各功能的 HTTP 接口（项目、文件、应用、看板） |
| 前端界面模块 | frontend/src/ | 工作台总览、AI 配置、对话工作台、两个集成应用首页 |
| 配置模块 | config/ | 全局设置（应用名、端口、路径等） |
| 构建脚本模块 | scripts/ | 引用文档模板生成、测试脚本 |

---

## 附录 D 核心功能对照表

为便于快速了解系统对财务流程的支撑，下表将"业务需求"与"系统功能"进行对照：

| 业务需求 | 系统功能 | 涉及章节 |
|---------|---------|---------|
| 需要核对两份 Excel 金额 | 数据核对与校验（六步向导） | 第七章 |
| 需要分析财务趋势并出报告 | 财务建模与分析（四步向导） | 第八章 |
| 需要对 Excel 做临时自由分析 | 对话工作台 | 第六章 |
| 需要配置 AI 模型 | AI 功能配置 | 第五章 |
| 需要管理项目文件 | 项目目录抽屉 | 第六章 |
| 需要安全写回原文件 | 受控写回（只改勾选单元格） | 第七章 |
| 需要导出专业报告 | 报告导出（Word / PDF / HTML） | 第八章 |

---

## 附录 E 业务流程详解

### E.1 数据核对完整业务流

数据核对与校验应用遵循"数据准备 → 匹配核对 → 审计决策 → 写回留痕"的完整业务流：

```
[数据准备] 选择依据表/分表 → 上传并物化数据
        ↓
[匹配核对] 设定匹配键 → 金额比对 → 差异分级（OK/A1/A2/B/C）
        ↓
[审计决策] 待审计清单逐条处理 → 选择补录/忽略/确认对应关系
        ↓
[写回留痕] 勾选差异 → 确认修改 → 写回原表（备份 .bak + 日志留痕）
        ↓
[结果输出] 导出核对报告 + 核对后 Excel
```

### E.2 财务建模完整业务流

财务建模与应用遵循"输入诉求 → 建模映射 → 图表生成 → AI 报告"的完整业务流：

```
[输入诉求] 描述分析目标 → 勾选多份 Excel
        ↓
[建模映射] AI 推荐模型 → 变量映射（可手动调整）
        ↓
[图表生成] 设置风格 → 自动生成图表（所见即所得）
        ↓
[AI 报告] 确定性计算 + AI 撰写 → 报告预览 → 导出 / 保存历史
```

### E.3 写回与审计机制详解

写回是系统最关键的受控操作，其保障机制包括：

| 机制 | 保障 |
|------|------|
| 只改勾选单元格 | 仅修改用户明确勾选的差异项，其余 100% 保留 |
| 自动备份 | 写回前生成 .bak 备份，可恢复原文件 |
| 字段级日志 | 记录修改前后值、执行人、时间，可追溯 |
| 工作模式 | 人工审批需确认；全自动直接执行 |
| 文件锁检测 | 文件被占用时提示关闭，不强行写入 |

---

## 附录 F 开发与部署技术说明

### F.1 技术栈明细

| 层级 | 技术 | 版本 | 作用 |
|------|------|------|------|
| 后端语言 | Python | 3.14 | 服务端逻辑 |
| 后端框架 | Flask | 3.1 | Web 服务 |
| 数据库 | SQLite | 内置 | 数据持久化（WAL） |
| 前端框架 | React | 18 | 用户界面 |
| 构建工具 | Vite | 6 | 前端打包 |
| 类型系统 | TypeScript | — | 类型安全 |
| 样式 | Tailwind CSS | 4 | 界面样式 |
| 图表 | ECharts | 5 | 数据分析图表 |
| 编辑器 | Vditor | 3.11 | Markdown 编辑渲染 |
| AI 编排 | smolagents | 1.26 | 智能体工具调用 |
| 文档转换 | Pandoc | 3.10 | 报告导出 |
| 图片转换 | cairosvg | 2.9 | SVG 转 PNG |

### F.2 部署环境

| 项 | 说明 |
|----|------|
| 操作系统 | Windows 10 / 11（64 位） |
| 运行时 | Python 3.14 + Flask，.NET Framework 4.0+ |
| 浏览器 | Chrome / Edge ≥ 115 |
| 网络 | 内网离线部署，数据本机保存 |
| 启动方式 | 双击 Finance Agent.exe 或 python app.py |

### F.3 服务端口

系统默认从 8080 端口启动，若被占用则自动顺延至 8085 之间的空闲端口。可通过命令行参数或环境变量指定端口。服务绑定 127.0.0.1，仅本机可访问。

### F.4 离线发布包

系统提供离线发布包（offline_packages 目录），内含运行所需的所有依赖，可在无外网环境下安装。包括 Python 依赖、Pandoc、cairosvg 等，确保离线环境可完整部署。

---

## 结语

**基于自然语言操控的财务标准化作业执行系统**（财务标准化作业系统 V1.0.0）是一套面向财务人员的智能工作台。它针对财务日常中重复性高、规则明确的核对与分析工作，提供"对话 + 集成应用"双形态解决方案：既保留了对话式的灵活自由，又通过集成应用将高频流程固化为向导式操作，让业务人员无需建模与编程知识即可产出专业结果。

系统以"AI 辅助决策、本地确定计算、全程可审计"为核心设计理念，通过隐藏项目组件复用主工作台基础设施，实现了高可维护性与可扩展性。无论是销售回款提成的季度核对、还是多文件财务建模分析，本系统都能显著提升效率、降低错误率、保障数据安全与操作可追溯。

我们相信，本系统能够成为财务人员值得信赖的智能助手，让财务工作更高效、更规范、更安心。
"""


def get_full_content():
    return DOC_MD + "\n\n" + DOC_MD_2 + "\n\n" + DOC_MD_3

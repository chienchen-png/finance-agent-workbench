# 智能财务工作台使用说明书

下载后点击桌面/文件夹内的 **`Finance Agent.exe`** 启动软件，系统会自动在浏览器中打开软件页面。本软件基于浏览器 Web 前端运行，请确保系统内 Chrome / Edge 浏览器的版本不低于 **115**。

---

## 一、首页 / 工作台总览

**页面截图描述**：
该图为软件首页"工作台总览"界面。左侧为全局导航栏，右侧主体部分展示了各项统计指标卡片（如可调用模型数、项目数等）、图表（近 7 日使用时长趋势图、活跃时段分布图）以及"最近项目"和"应用入口"快捷跳转区。

**功能说明**：
首页默认进入"工作台总览"，是左侧导航栏的第一部分。它主要提供三类能力：

- **指标总览**：以统计卡片展示系统当前状态（可调用模型数、项目数、集成应用数、今日使用时长、今日运行次数），便于随时掌握使用情况。
- **趋势分析**：通过折线图/柱状图呈现近 7 日使用时长趋势与一天各时段活跃分布，辅助了解工作节奏。
- **快捷跳转**：在"最近项目"区点击项目直接进入对应对话窗口；在"应用入口"区一键进入两款集成应用。

---

## 二、AI 功能配置

**页面截图描述**：
该图为左侧导航栏第二部分"AI 功能配置"界面。页面顶部有"模型配置""智能体配置""Skill 配置"三个标签页，当前处于"模型配置"页面。页面列出了 DeepSeek（公网）、通义千问（内网部署）、第三方提供商等模型供应商，以及各供应商下的具体模型与配置状态。

**功能说明**：
左侧导航栏第二部分为"AI 功能配置"页面，包含三个标签页：

1. **模型配置**：支持配置模型供应商及每个模型的具体功能（类型、能力、上下文窗口等）。软件**内置了 DeepSeek 官方模型与公司本地部署的模型，且已配置完毕**——在内网运行环境下，此页面可直接忽略，无需额外操作。若需调整可在此新增供应商、编辑模型或切换默认模型。
2. **智能体配置**：静态展示，仅用于查看智能体的执行模式、迭代次数等参数，用户不可更改。
3. **Skill 配置**：静态展示，仅用于查看已加载的技能模块及流程步骤，用户不可更改。

---

## 三、新建项目 / 项目空间

**新建项目弹窗截图描述**：
该图为项目空间页面的"新建项目"弹窗。用户需填写"项目名称"（如：测试 - 2025 年抽查记录）、"工作目录"（选择路径文件夹），以及"默认模型"（此处默认选择 DeepSeek-v4-Flash），点击确认即可创建。

**功能说明**：
左侧导航栏第三部分是**项目空间**，它承载着 AI 对话的主要入口。

- **项目即对话窗口**：每个项目代表一个独立的 AI 对话窗口，不同项目之间的**上下文、数据库、项目目录互不共享**，数据彼此隔离。
- **新建项目**：点击项目列表右侧的"＋"即可新建项目。新建页面需依次填写：
  - **项目名称**：给用户看的名称，清晰明了即可，无格式内容要求。例如"测试 - 2025 年抽查记录"。
  - **工作目录**：智能体可操作的路径文件夹。智能体能够在这个文件夹下阅读文件、操作/修改文件、新建文件。
  - **默认模型**：AI 对话使用的模型。内网环境下默认选择公司本地部署的 **"qwen"** 模型。
- 填写完成后点击"创建"即可建立新项目。

---

## 四、项目对话窗口

**界面截图描述**：
该图展示了进入项目后的对话窗口。窗口上方展示了一些预设的提示词（如核对逻辑、生成分析报告等），方便用户快速开始。底部为对话框和输入区，左侧有模型选择框、工作模式选择、项目目录、项目数据库等入口。

**功能说明**：
进入项目对话窗口后，有两种发起需求的方式：

- **点击预设提示词快速开始**：窗口上方预置了常见任务的提示词（如"核对逻辑""生成分析报告"等），点一下即可快速发起。
- **自定义发送需求**：在底部输入区直接输入自己的需求，再点击发送即可。

对话窗口中 AI 会根据你的需求调用项目目录内的文件、项目数据库中的数据，执行分析并返回结果。

---

## 五、对话窗口底部工具栏

**截图描述**：
该图展示了对话框底部的工具栏细节。从左到右依次是模型选择框（下拉菜单，图中显示了 DeepSeek、Claude、Qwen 等多个可选模型），用户可以选择不同模型完成每次对话，内网环境下默认选择"qwen"。

**功能说明**：
对话框底部工具栏从左到右依次是**模型选择框**。用户可为每次对话选择不同的 AI 模型（如 DeepSeek、Claude、Qwen 等）。**在内网环境下，默认选择公司本地部署的"qwen"模型**，以确保数据不出域、运行稳定。

---

## 六、工作模式选择与项目目录

**截图描述**：
该图展示了工具栏中"工作模式"和"项目目录"两个选项。工作模式提供"手动"和"全自动"两种：手动模式下 AI 只能在预定的项目目录中读取和操作文件，工作目录外的操作每次需手动确认；全自动模式下 AI 除特别危险命令外拥有其余所有权限，**非技术人员建议启用全自动模式**。项目目录选项支持查看、上传、预览和引用文件。

**功能说明**：

**工作模式选择**（两种模式）：
- **手动模式**：AI 只能在预定的项目目录中读取和操作文件；若操作范围超出工作目录，每次都需要用户手动确认。该模式安全边界更严，适合对操作范围有严格要求的场景。
- **全自动模式**：AI 除特别危险命令外，拥有其余所有权限，可更高效地完成任务。**非技术人员建议启用全自动模式**，以减少反复确认、提升效率。

**项目目录**：点击可查看当前项目的文件夹结构，支持上传文件、双击预览文件、右键进一步引用文件等操作，是管理 AI 可操作文件的核心入口。

---

## 七、项目目录与数据库管理

**截图描述**：
该图展示了"项目目录"和"项目数据库"两个界面。项目目录显示了当前项目的文件夹结构，支持上传文件、双击预览、右键引用操作。项目数据库用于存储所有需要 AI 处理的数据文件（如 Excel 等），每个数据库文件支持索引、引用、写回等功能，用户可以通过查看**索引**判断数据是否解析正确。

**功能说明**：

- **项目目录**：即创建项目时选择的目录文件夹。点击可进入具体目录查看文件，支持上传；双击文件可预览；右键文件可进一步执行引用等操作。
- **项目数据库**：所有需要 AI 处理的数据文件（如 Excel 等），均需先上传到项目数据库中。
  - **自动上传**：若未上传，直接引用项目目录中的文件，AI 会自动将其上传到数据库。
  - **核心能力**：每个数据库文件支持**索引、引用、写回**等功能。用户可通过查看**索引**（即数据表的结构信息，如有哪些列、类型、样例）来判断数据是否解析正确。

---

## 八、数据核对与校验（集成应用一）

> 左侧导航栏第四部分是 **AI 集成应用**。每个应用内是图形化选择窗口，用户按指定流程输入参数、即可执行并获取结果。每个集成应用上侧有**应用介绍**和**模型选择框**（内网环境下默认选择 qwen）。

### 步骤 1：选择数据文件

**截图描述**：
该图展示了"数据核对与校验"应用的第一步。左侧为该应用的功能流程导航，右侧为操作区。用户需在右侧项目数据库内上传所有需要的数据文件，然后选定"依据表（核对的依据文件，如实发金额等）"和"待核对表（核对的处理对象文件，如应发金额等）"，点击下一步。

**功能说明**：
进入应用一"数据核对与校验"后，第一步在右侧"项目数据库"内上传所有需要的数据文件，然后依次选定：

- **依据表**：核对的依据文件（例如"实发金额"表）。
- **待核对表**：核对的处理对象文件（例如"应发金额"表）。

选定后点击"下一步"继续。

### 步骤 2：选择匹配键

**截图描述**：
该图展示了应用流程第二步，用户需选择匹配键（即通过什么变量来匹配同一条数据），例如选择"个人回款总额"或"销售人员"，确认后点击下一步。

**功能说明**：
接着选择**匹配键**，即通过哪个变量把两张表的同一条数据对应起来。例如可选定"个人回款总额"或"销售人员"作为匹配依据。确认后点击"下一步"继续。

### 步骤 3：选择核对变量

**截图描述**：
该图展示了应用流程第三步，用户需分别选择核对变量，即依据表的哪个数据要与待核对表的数据核对，例如在表中勾选对应的字段，确认后点击下一步。

**功能说明**：
分别选择**核对变量**，确定"依据表的哪个数据"需要与"待核对表的哪个数据"进行核对。例如在表中勾选相应字段。确认后点击"下一步"继续。

### 步骤 4：设定容差策略与初步报告

**截图描述**：
该图展示了流程的后续步骤和结果。首先设定容差策略（选择容差值，以及 AI 对未匹配数据应该怎么处理），确认后 AI 自动运行查找。运行后 AI 自动编写核对校验报告，报告中展示了差异分级结果（如 A1、A2、B、C、OK 等类别及数量），并提供详细的待确认处理问题列表。

**功能说明**：
然后设定**容差策略**：

- 选择**容差值**（金额相差多少以内算"匹配"）。
- 指定 **AI 对未匹配数据的处理方式**。

确认后点击"下一步"，AI 自动运行查找。运行完成后，AI 自动编写**核对校验报告**，报告中展示差异分级结果（如 A1、A2、B、C、OK 等类别及各自数量），并列出**待确认处理的问题列表**，供用户逐条处理。

### 步骤 5：确认处理与迭代匹配

**截图描述**：
该图展示了 AI 运行完成后的处理页面。用户需根据 AI 的差异分级结果（如"金额差异过大""除金额均一致"等）为每条问题数据选择处理方式（如"更正异常数据""无需处理"等），确认后点击下一步进行进一步匹配。该过程支持迭代匹配，直到匹配达到需求后进入数据修改页面。

**功能说明**：
根据 AI 的差异分级结果，为每条问题数据选择处理方式（例如"金额差异过大""除金额均一致"等），并指定处理动作（如"更正异常数据""无需处理"等）。确认每条处理方式后点击"下一步"进一步匹配。该过程支持**迭代匹配**，直至匹配达到需求后进入数据修改页面（过程中随时可进入修改）。

### 步骤 6：确认写回与报告输出

**截图描述**：
该图展示了数据处理完成后的"确认写回"阶段。用户确认修正结果后，系统将生成最终的核对报告，报告中包含核对结果、修正记录、原始数据备份和修改日志等信息，并支持查验或导出。

**功能说明**：
根据 AI 的差异分级结果依次处理问题数据后，点击"确认写回"，即得到最终输出。可打开查验、或导出核对报告（含核对结果、修正记录、数据备份与修改日志等）。

---

## 九、财务建模与分析（集成应用二）

### 步骤 1：上传数据与输入需求

**截图描述**：
该图展示了第二个集成应用"财务建模与分析"的初始界面。用户首先上传所需分析数据，然后输入分析需求（大致方向），点击下一步开始建模。系统会自动匹配数据充足且相关的财务模型，用户可以自主勾选 AI 推荐的模型或下方模型库内预置的模型，每种模型都提供了详细的应用规则和场景介绍。

**功能说明**：
第二个集成应用为"财务建模与分析"。进入后：

1. 上传所需分析数据。
2. 输入分析需求（大致方向）。
3. 点击"下一步"开始建模。

AI 自动匹配数据充足且相关的财务模型。用户可自主勾选 **AI 推荐的模型**，或从**下方模型库**内预置的模型中选择。每种模型都提供了详细的应用规则和场景介绍。

### 步骤 2：变量映射与数据清洗

**截图描述**：
该图展示了模型确认后的"变量映射"环节。系统会自动将上传的数据映射至模型所需变量中，软件也提供数据清洗与合并的基本工具，支持派生变量（用户自定义公式生成新变量），需求确认后进入下一步。

**功能说明**：
确认模型后进入**变量映射**环节：

- **自动映射**：AI 自动将上传的数据映射至模型所需的变量。
- **数据清洗与合并**：软件提供基础的数据清洗与合并工具。
- **派生变量**：支持用户自定义公式生成新变量（如"利润 = 收入 - 成本"）。

确认需求后点击"下一步"继续。

### 步骤 3：图表风格选择

**截图描述**：
该图展示了图表风格选择页面。用户可以选择在分析报告中插入的图表类型和样式，软件内置大量丰富的图表模板并提供各种变体和配色风格选择，也提供了图表适配数据和预览样式，用户选择后点击下一步。

**功能说明**：
下一步为**图表风格选择**：用户可选择在最终分析报告中插入的图表类型与样式。软件内置大量丰富的图表模板，提供各种变体和配色风格选择，并为图表配有适配数据与预览样式。选定后点击"下一步"。

### 步骤 4：生成报告与结果查看

**截图描述**：
该图展示了 AI 运行完成后的最终分析报告。报告包含完整的分析数据、图表和结论，支持在线编辑与导出。下方的"历史记录"中会存入该报告，用户可以随时查看或删除。

**功能说明**：
AI 运行完成后会输出一份完整的、附带图表的**分析报告**，支持在线编辑与导出。历史报告会存入下方"历史记录"中，用户可随时查看或删除。
